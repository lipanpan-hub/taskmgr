---
trigger: always_on
---

# 架构重构与路径迁移检查原则 (Agent Experience Rules)

- **全局重构防漏查原则**：在针对项目代码执行目录迁移、架构调整或文件重命名操作时，**切勿将搜索与替换范围局限于 `src/` 等纯业务代码目录下**。务必在整个项目范围（包括根目录）进行审查，因为根目录下的关键配置文件（如 `drizzle.config.ts`、`tsconfig.json`、`package.json` 等）极大概率也引用了这些被改变的路径。只有确保这些配置文件被同步更新，才能算完成了一次完整的重构。



